npm init -y
yarn add mongoose

Please refer to the recordings for shell setup
In Shell: 
mongoimport --jsonArray path -d database -c collection
mongoimport --jsonArray JSON\courses.json -d academic -c courses
use academic
for (let col in db.courses.findOne()) {print(col)}

Note: Each record is considered a document, and each table is refered to as a collection