<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>Express on VASH</title>
    </head>
    <body>
        <!-- Introduction to Vash -->
        <h1>@model.title</h1>
        <p>From Router With <b>Love</b>. @model.msg</p>

        <!-- Example of how easy it is to use JavaScript in vash. kirbysayshi <3 -->
        @if(model.age == 21) {
        <h2>You should see this if you're @model.age</h2>
        }

        <!-- Example of a for loop -->
        <!-- <div>
            @for(var i = 0; i < 3; i++){
            <div>I be an item! @i</div>
            }
        </div> -->

        @model.users.forEach(function(user){
        <div>@user.firstName</div>
        })
    </body>
</html>
